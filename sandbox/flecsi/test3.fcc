#include <iostream>
#include <vector>
using namespace std;

int main(int argc, char** argv){
  std::vector<int> v = {0, 1, 2, 3, 4, 5};
 
  forall(const int& i : v) // access by const reference
    v[i] = i;

  return 0;
}
